#!/usr/bin/env bash

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/../../.." && pwd)"

export BOUKENSHA_DIR="$(cd "$REPO_ROOT/.boukensha" && pwd)"

cd "$SCRIPT_DIR/../../ruby/07_the_run_dsl"
bundle exec ruby examples/example.rb